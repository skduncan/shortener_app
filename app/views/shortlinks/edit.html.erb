<% provide(:title, 'Edit Short Link') %>
<% provide(:button_text, 'Save changes') %>
<% provide(:form_path, shortlink_path) %>

<h1>Edit a new shortened link</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>